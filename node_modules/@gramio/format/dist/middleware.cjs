'use strict';

var mutator = require('./mutator-DnoFwAyW.cjs');

const formatMiddleware = (context, next) => {
  mutator.FormattableMap[context.method]?.(context.params);
  return next();
};

exports.formatMiddleware = formatMiddleware;
