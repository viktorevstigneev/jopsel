import { a as FormattableMap } from './mutator-CK5h8wA4.js';

const formatMiddleware = (context, next) => {
  FormattableMap[context.method]?.(context.params);
  return next();
};

export { formatMiddleware };
