import { b as isMediaUpload, e as extractFilesToFormData } from './utils-DudYgHGK.js';

const filesMiddleware = async (context, next) => {
  if (context.params && isMediaUpload(context.method, context.params)) {
    const [formData, rest] = await extractFilesToFormData(
      context.method,
      context.params
    );
    if (formData) context.formData = formData;
    context.params = rest;
  }
  return next();
};

export { filesMiddleware };
